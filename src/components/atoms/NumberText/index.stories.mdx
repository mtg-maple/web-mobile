import { action } from "@storybook/addon-actions";
import { Story, Meta, Preview, Props, Source, Description } from "@storybook/addon-docs/blocks";

import NumberText from '../NumberText';

<Meta
  title="Atoms/NumberText"
  parameters={{
    component: NumberText,
  }}
/>

# NumberText

<Source code={`
    import NumberText from '@mtg-maple/web-components/atoms/NumberText';
     
    export default = (props) => (
        <NumberText num={4} size="large" color="muteText"/>
    );
`} />

<Props of={NumberText} />

## Default

<Preview withSource="open">
  <Story name="default" >
    <NumberText num={1}/>
  </Story>
</Preview>

## Size

<Preview withSource="open">
  <Story name="small" >
    <NumberText num={1} size="small"/>
  </Story>
  <Story name="medium" >
    <NumberText num={2} size="medium"/>
  </Story>
  <Story name="large" >
    <NumberText num={3} size="large"/>
  </Story>
</Preview>

## Color

<Preview withSource="open">
  <Story name="text" >
    <NumberText num={1} color="text"/>
  </Story>
  <Story name="muteText" >
    <NumberText num={2} color="muteText"/>
  </Story>
</Preview>
