import { action } from "@storybook/addon-actions";
import { Story, Meta, Preview, Props, Source } from "@storybook/addon-docs/blocks";
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faSearch } from '@fortawesome/free-solid-svg-icons';

import Button from '../Button';

<Meta
  title="Atoms/Button/Text"
  parameters={{
    component: Button,
  }}
/>

# Button

<Source code={`
import Button from '@mtg-maple/web-components/atoms/Button';
	
export default = (props) => (
	<Button type="text" onClick={() => alert('clicked')}>
    Basic Button
  </Button>
);
`} />

<Props of={Button} />

## Basic

<Preview withSource="open">
	<Story name="Basic">
		<Button type="text" onClick={() => alert('clicked')}>
      Basic Button
    </Button>
  </Story>
</Preview>

## Contextual Classes
<Preview withSource="open">
	<Story name="Primary">
		<Button contextualClass="primary" onClick={() => alert('clicked')}>
      Primary
    </Button>
  </Story>
  <Story name="Secondary">
		<Button contextualClass="secondary" onClick={() => alert('clicked')}>
      Secondary
    </Button>
  </Story>
</Preview>

## Sizes
<Preview withSource="open">
	<Story name="Small">
		<Button size="small" onClick={() => alert('clicked')}>
      Small
    </Button>
  </Story>
  <Story name="Medium">
		<Button size="medium" onClick={() => alert('clicked')}>
      Medium
    </Button>
  </Story>
  <Story name="Large">
		<Button size="large" onClick={() => alert('clicked')}>
      Large
    </Button>
  </Story>
</Preview>
