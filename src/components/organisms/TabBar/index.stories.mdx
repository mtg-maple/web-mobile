import { useState } from 'react';
import { action } from "@storybook/addon-actions";
import { Story, Meta, Preview, Props, Source, Description } from "@storybook/addon-docs/blocks";
import {
  BrowserRouter as Router,
  Switch,
  Route,
} from 'react-router-dom';

import { faHome, faSearch, faUser } from '@fortawesome/free-solid-svg-icons'


import TabBar from '../TabBar';

<Meta
  title="Organisms/TabBar"
  parameters={{
    component: TabBar,
  }}
/>

# TabBar

<Source code={`
    import TabBar from '@mtg-maple/web-components/organisms/TabBar';
     
    export default = (props) => {
      return <TabBar buttons={[
        { icon: faHome },
        { icon: faSearch },
        { icon: faUser },
      ]}/>;
    };
`} />

<Props of={TabBar} />

## Basic

<Preview withSource="open">
  <Story name="Basic" >
    <Router>
      <Switch>
        <Route path='/home'><p>HOME</p></Route>
        <Route path='/search'><p>SEARCH</p></Route>
        <Route path='/mypage'><p>MYPAGE</p></Route>
      </Switch>
      <TabBar links={[
        { icon: faHome, link: '/home' },
        { icon: faSearch, link: '/search' },
        { icon: faUser, link: '/mypage' },
      ]}/>
    </Router>
  </Story>
</Preview>

## OnClick

<Preview withSource="open">
  <Story name="onClick" >
    <Router>
      <Switch>
        <Route path='/home'><p>HOME</p></Route>
        <Route path='/search'><p>SEARCH</p></Route>
        <Route path='/mypage'><p>MYPAGE</p></Route>
      </Switch>
      <TabBar links={[
        { icon: faHome, link: '/home', onClick: () => alert('home clicked') },
        { icon: faSearch, link: '/search', onClick: () => alert('search clicked')  },
        { icon: faUser, link: '/mypage', onClick: () => alert('mypage clicked')  },
      ]}/>
    </Router>
  </Story>
</Preview>
