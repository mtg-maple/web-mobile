import { action } from "@storybook/addon-actions";
import { Story, Meta, Preview, Props, Source } from "@storybook/addon-docs/blocks";
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faSearch } from '@fortawesome/free-solid-svg-icons';

import Button from '../Button';

<Meta
  title="Atoms/Button/Icon"
  parameters={{
    component: Button,
  }}
/>

# Button

<Source code={`
import Button from '@mtg-maple/web-components/atoms/Button';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faSearch } from '@fortawesome/free-solid-svg-icons';
	
export default = (props) => (
	<Button type="icon" onClick={() => alert('clicked')}>
    <FontAwesomeIcon icon={faSearch}/>
  </Button>
);
`} />

<Props of={Button} />

## Basic

<Preview withSource="open">
	<Story name="Basic">
		<Button type="icon" onClick={() => alert('clicked')}>
      <FontAwesomeIcon icon={faSearch}/>
    </Button>
  </Story>
</Preview>

## Contextual Classes
<Preview withSource="open">
	<Story name="Primary">
		<Button type="icon" contextualClass="primary" onClick={() => alert('clicked')}>
      <FontAwesomeIcon icon={faSearch}/>
    </Button>
  </Story>
  <Story name="Secondary">
		<Button type="icon" contextualClass="secondary" onClick={() => alert('clicked')}>
      <FontAwesomeIcon icon={faSearch}/>
    </Button>
  </Story>
</Preview>

## Sizes
<Preview withSource="open">
	<Story name="Small">
		<Button type="icon" size="small" onClick={() => alert('clicked')}>
      <FontAwesomeIcon icon={faSearch}/>
    </Button>
  </Story>
  <Story name="Medium">
		<Button type="icon" size="medium" onClick={() => alert('clicked')}>
      <FontAwesomeIcon icon={faSearch}/>
    </Button>
  </Story>
  <Story name="Large">
		<Button type="icon" size="large" onClick={() => alert('clicked')}>
      <FontAwesomeIcon icon={faSearch}/>
    </Button>
  </Story>
</Preview>
